Facebook Album Browser is an RTCamp assignment.

It uses the Facebook Javascript and PHP sdk to login the user and then uses the Facebook Graph API to fetch user data.

It uses Bootstrap for layout.

